import 'package:flutter/material.dart';

class HomeWidget extends StatelessWidget {

  HomeWidget();

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("This is your profile. Stay tuned!"),
    );
  }
}