import 'package:flutter/material.dart';

class FollowingWidget extends StatelessWidget {

  FollowingWidget();

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("These are the people you follow. Stay tuned!"),
    );
  }
}