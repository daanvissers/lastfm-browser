import 'package:flutter/material.dart';

class LibraryWidget extends StatelessWidget {

  LibraryWidget();

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("This is your Library. Stay tuned!"),
    );
  }
}